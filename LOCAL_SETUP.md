# 本地端安裝與測試指南

## ✅ 打包完成!

插件已成功打包為: **`release/gasker-content-refresher-1.0.0.zip`** (27KB)

## 🚀 本地端 WordPress 安裝步驟

### 方式 1: 透過 WordPress 後台上傳 (推薦)

1. **前往你的本地 WordPress 網站**
   ```
   例如: http://localhost:8888/wp-admin
   或: http://localhost/wordpress/wp-admin
   ```

2. **上傳插件**
   - 登入 WordPress 後台
   - 前往 **插件 → 安裝插件**
   - 點擊頁面頂部的 **「上傳插件」** 按鈕
   - 點擊 **「選擇檔案」**
   - 選擇: `/Users/gask/.claude-worktrees/wordpress自動改寫文章/zen-taussig/release/gasker-content-refresher-1.0.0.zip`
   - 點擊 **「立即安裝」**

3. **啟用插件**
   - 安裝完成後,點擊 **「啟用插件」**
   - 或前往 **插件 → 已安裝的插件** → 找到 "Gasker Content Refresher" → 點擊 **「啟用」**

### 方式 2: 直接複製到插件目錄

```bash
# 1. 解壓縮插件
cd /Users/gask/.claude-worktrees/wordpress自動改寫文章/zen-taussig/release
unzip gasker-content-refresher-1.0.0.zip

# 2. 複製到 WordPress 插件目錄
# 請將 /path/to/wordpress 替換成你的 WordPress 安裝路徑
cp -r gasker-content-refresher /path/to/wordpress/wp-content/plugins/

# 範例路徑:
# MAMP: /Applications/MAMP/htdocs/wordpress/wp-content/plugins/
# Local by Flywheel: ~/Local Sites/your-site/app/public/wp-content/plugins/
# XAMPP: /Applications/XAMPP/htdocs/wordpress/wp-content/plugins/

# 3. 設定權限
chmod -R 755 /path/to/wordpress/wp-content/plugins/gasker-content-refresher
```

然後前往 WordPress 後台 → 插件 → 啟用 "Gasker Content Refresher"

### 方式 3: 使用 WP-CLI (如果已安裝)

```bash
# 進入 WordPress 目錄
cd /path/to/wordpress

# 安裝插件
wp plugin install /Users/gask/.claude-worktrees/wordpress自動改寫文章/zen-taussig/release/gasker-content-refresher-1.0.0.zip

# 啟用插件
wp plugin activate gasker-content-refresher

# 檢查插件狀態
wp plugin list | grep gasker
```

## 📋 安裝前置需求

### 1. 安裝 AI Engine 插件 (必須)

**方式 A: WordPress 後台**
```
1. 前往 插件 → 安裝插件
2. 搜尋 "AI Engine"
3. 找到作者為 "Jordy Meow" 的版本
4. 點擊「立即安裝」
5. 點擊「啟用」
```

**方式 B: WP-CLI**
```bash
wp plugin install ai-engine --activate
```

### 2. 設定 AI Engine API Key

1. **前往 AI Engine 設定**
   - WordPress 後台 → **Meow Apps → AI Engine**
   - 點擊 **Settings** 標籤

2. **設定 OpenAI API Key** (推薦)
   - 點擊 **OpenAI** 標籤
   - 輸入你的 OpenAI API Key
   - 如果沒有,前往 https://platform.openai.com/api-keys 申請
   - 點擊 **Save**

   或

3. **設定 Google Gemini API Key**
   - 點擊 **Google** 標籤
   - 輸入你的 Gemini API Key
   - 如果沒有,前往 https://makersuite.google.com/ 申請
   - 點擊 **Save**

## ⚙️ 配置 Gasker Content Refresher

### 1. 前往設定頁面

WordPress 後台 → **設定 → Content Refresher**

### 2. 基本配置 (建議值)

```
每日處理篇數: 5
冷卻天數: 30
最大文章長度: 5000
```

### 3. AI 設定

```
AI 模型: GPT-4o (推薦,品質最佳)
目標語言: 繁體中文
```

### 4. 發布模式 (重要!)

```
⚪ 自動發布 (直接更新已發布文章)
🔘 待審閱 (改寫後設為待審狀態) ← 初期強烈建議
```

### 5. 排除設定 (選用)

如果你有不想被改寫的分類或文章:
```
排除分類: [選擇分類]
排除文章 ID: 1, 5, 10 (用逗號分隔)
```

### 6. Email 通知 (選用)

```
☑ 啟用通知
通知信箱: your-email@example.com
```

### 7. 儲存設定

點擊頁面底部的 **「儲存設定」** 按鈕

## 🧪 測試運行

### 1. 手動測試

1. 確保你的 WordPress 網站有至少幾篇已發布的文章
2. 在 Content Refresher 設定頁面,找到右側的 **「手動操作」** 區塊
3. 點擊 **「立即執行一次」** 按鈕
4. 等待 30-60 秒 (取決於文章數量和長度)
5. 查看執行結果

### 2. 檢查執行日誌

在設定頁面下方的 **「執行日誌」** 區域:
- 查看處理了哪些文章
- 檢查成功/失敗/跳過的數量
- 點擊 **「查看」** 按鈕展開詳細資訊
- 確認 Token 消耗量

### 3. 檢查改寫結果

1. 前往 **文章 → 所有文章**
2. 篩選狀態為 **「待審閱」** 的文章
3. 編輯文章查看改寫內容
4. 檢查:
   - ✅ 文字是否已現代化
   - ✅ HTML 標籤是否完整 (h2, h3, ul, li 等)
   - ✅ 圖片是否保留 (img src)
   - ✅ 連結是否保留 (a href)
   - ✅ 內容長度是否合理
5. 如果滿意,點擊 **「發布」** 按鈕

## 🔍 檢查系統狀態

### 1. 插件狀態

在 Content Refresher 設定頁面右側的 **「系統狀態」** 區塊:

```
AI Engine: ✓ 已啟用
排程狀態: ✓ 已啟用
下次執行: 2025-01-25 10:00
```

如果顯示 "✗ 未啟用",請檢查:
- AI Engine 插件是否已安裝並啟用
- API Key 是否已設定

### 2. 檢查 Cron 排程 (進階)

**使用 WP-Crontrol 插件** (推薦):
```
1. 安裝 WP-Crontrol 插件
2. 前往 工具 → Cron 事件
3. 搜尋 "gcr_daily_rewrite_event"
4. 確認事件已註冊且有下次執行時間
```

**使用 WP-CLI**:
```bash
# 列出所有 Cron 任務
wp cron event list

# 應該看到:
# gcr_daily_rewrite_event    daily    ...

# 手動觸發 Cron 任務
wp cron event run gcr_daily_rewrite_event
```

## 📊 本地測試數據準備

如果你的本地 WordPress 沒有足夠的文章:

### 方式 1: 手動創建測試文章

```bash
# 使用 WP-CLI 快速創建測試文章
for i in {1..10}; do
  wp post create \
    --post_type=post \
    --post_status=publish \
    --post_title="測試文章 $i" \
    --post_content="<h2>這是標題</h2><p>這是測試內容。</p><img src='https://via.placeholder.com/400' alt='測試圖片'><ul><li>項目一</li><li>項目二</li></ul>" \
    --post_date="2024-01-0$i 10:00:00"
done
```

### 方式 2: 匯入範例內容

WordPress 提供官方測試資料:
```
1. 下載: https://codex.wordpress.org/Theme_Unit_Test
2. 前往 工具 → 匯入 → WordPress
3. 上傳 XML 檔案
4. 匯入所有內容
```

## 🐛 故障排除

### 問題 1: 插件啟用後出現錯誤

**可能原因**: PHP 版本過低

**解決方法**:
```bash
# 檢查 PHP 版本
php -v

# 需要 PHP 7.4 或更高版本
# 如使用 MAMP,可在 MAMP 設定中切換 PHP 版本
```

### 問題 2: AI Engine 未啟用警告

**解決方法**:
```bash
# 確認 AI Engine 已安裝
wp plugin list | grep ai-engine

# 如果未安裝
wp plugin install ai-engine --activate
```

### 問題 3: 執行時出現 API 錯誤

**可能原因**:
- API Key 未設定或錯誤
- API 帳號無餘額
- 網路連線問題

**解決方法**:
1. 檢查 AI Engine 設定中的 API Key
2. 前往 OpenAI/Google 後台檢查帳號餘額
3. 測試網路連線

### 問題 4: Cron 任務未執行

**可能原因**: 本地開發環境 WP-Cron 可能不會自動執行

**解決方法**:
```bash
# 手動觸發 Cron
wp cron event run gcr_daily_rewrite_event

# 或在瀏覽器訪問:
curl http://localhost:8888/wp-cron.php?doing_wp_cron
```

### 問題 5: 找不到設定頁面

**解決方法**:
```
1. 確認插件已啟用
2. 清除瀏覽器快取
3. 前往: WordPress 後台 → 設定 (Settings) → Content Refresher
4. 如果還是找不到,檢查是否有權限 (需要管理員帳號)
```

## 🔒 開發除錯模式

如果需要除錯,編輯 `wp-config.php`:

```php
// 啟用除錯模式
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);

// 日誌檔案位置: wp-content/debug.log
```

查看日誌:
```bash
tail -f /path/to/wordpress/wp-content/debug.log
```

## 📝 本地測試檢查清單

- [ ] WordPress 6.0+ 已安裝
- [ ] PHP 7.4+ 運行中
- [ ] AI Engine 插件已安裝並啟用
- [ ] OpenAI/Gemini API Key 已設定
- [ ] Gasker Content Refresher 已上傳並啟用
- [ ] 設定頁面可正常訪問
- [ ] 基本參數已配置
- [ ] 系統狀態顯示正常
- [ ] 手動執行測試成功
- [ ] 執行日誌有記錄
- [ ] 改寫後的文章品質良好
- [ ] HTML 結構完整
- [ ] 圖片未遺失
- [ ] Cron 排程已註冊

## 🎯 下一步

本地測試成功後:

1. **第一週**: 使用待審閱模式,每天人工檢查改寫品質
2. **第二週**: 根據效果調整參數 (篇數、冷卻期等)
3. **第三週**: 如果品質穩定,可考慮啟用自動發布模式
4. **部署生產**: 確認一切正常後,部署到正式網站

## 📞 需要協助?

- 查看 `README.md` - 完整功能說明
- 查看 `INSTALL.md` - 詳細安裝指南
- 查看執行日誌中的錯誤訊息
- 檢查 `wp-content/debug.log`

---

**本地安裝完成!** 現在你可以開始測試 Gasker Content Refresher 的所有功能了。🎉

**插件位置**: `/Users/gask/.claude-worktrees/wordpress自動改寫文章/zen-taussig/release/gasker-content-refresher-1.0.0.zip`
